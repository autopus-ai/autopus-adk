# Plan

1. Freeze 12 distinct exact-source mutations; verify original PASS/seeded
   behavioral FAIL without model calls. Remove duplicates before any model trial.
2. Generate three surfaces, fix model/effort, measure advertised exposure.
3. Validate observer, scope guard, grading and runtime permissions. Grade seeded
   independent copies with immutable original tests; missing files cannot pass.
4. Freeze task hashes, oracle/source version, environment, order, arm hashes.
5. Run 36 serial trials, retain failures and incomplete usage, no intervention.
6. Report descriptive paired outcomes and limitations; do not change defaults
   without sufficient evidence. Separate evidence artifacts from source control.

Owners: A/B corpus workers, observer/report worker, root runner/generation and
integration; independent review for grading leakage and execution permissions.
