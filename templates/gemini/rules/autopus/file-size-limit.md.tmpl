---
name: file-size-limit
description: Project-specific source size policy and cohesion review
category: structure
platform: antigravity-cli
---

@import content/rules/file-size-limit.md

## This Project

{{if gt .MaxFileLines 0}}`architecture.max_file_lines` is declared as {{.MaxFileLines}}. `auto check --arch` enforces it; split before committing a source file that exceeds it.{{else}}`architecture.max_file_lines` is not declared, so source size is advisory here. Split only on a real independent concern.{{end}}

## Exclusions

- SPEC Markdown files under `.autopus/specs/**`: `prd.md`, `spec.md`, `plan.md`, `acceptance.md`, `research.md`, `review.md`
{{range .Exclusions}}- {{.Description}}: `{{.Pattern}}`
{{end}}
